// existing content ...

## EventServiceConfiguration
The `EventServiceConfiguration` class provides extension methods for configuring event-based services in the pipeline. It allows for the addition of event publishing and subscription infrastructure, as well as the registration of default subscribers. Here's an example of how to use it:

## PipelineConfigurationBuilder
The `PipelineConfigurationBuilder` class is a fluent builder for constructing pipeline configurations. It provides a convenient way to set up pipeline parameters, such as buffer configuration, windowing configuration, performance configuration, and quality configuration. Here's an example of how to use it:
